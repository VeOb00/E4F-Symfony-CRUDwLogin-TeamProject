{% extends 'pages/admin-temp.html.twig' %}

{% block title %}Edit Cause{% endblock %}
{% block admintitle %}Edit Cause{% endblock %}
{% block adminsubtitle %}{{ cause.title }}{% endblock %}

{% block admin %}
<div class="row">
    <div class="form-group col-lg-10">
    {{ include('cause/_form.html.twig', {'button_label': 'Update'}) }}
    </div>
    <div class="col-lg-2 mx-auto">
        <a class="btn btn-outline-secondary btn-block" href="{{ path('cause_index') }}">back to list</a>
        {{ include('cause/_delete_form.html.twig') }}
    </div>
</div>

{% endblock %}
