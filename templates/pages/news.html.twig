{% extends 'base.html.twig' %}

{% block title %}News{% endblock %}

{% block body %}
    <br>
    hello from the newspage
    <br>
    i will hold all the blogposts/ A news feed where we can put news, updates, current (strike) dates...
    <br>

    <div class="row">
        {% for post in posts %}
            <div class="col-md-6 col-lg-4 mb-5 ">
                <img src="{{ post.imagemain }}" alt="">
                <h3>{{ post.title }}</h3>
                <a href="{{ path('members_show', {'id': post.id}) }}"><small class="text-muted">More info</small></a>
            </div>
        {% else %}
            <div>
                <p>no records found</p>
            </div>
        {% endfor %}
    </div>

{% endblock %}